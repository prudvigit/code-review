define view ZV_EINA_EINE_SINGLE as select from eina
  inner join eine on eine.infnr = eina.infnr
{
  eina.matnr,
  eine.werks,
  eina.lifnr,
  eine.infnr,
  max(eine.aedat) as LatestChangedDate
}
group by eina.matnr, eine.werks, eina.lifnr, eine.infnr
